"""Player identity sub-package — alias resolution, strength scoring, archetype history.

Story 1 (identity):
    from legacy_engine.analytics.players.identity import (
        load_alias_map, resolve_player,
        materialize_player_aliases, suggest_aliases, AliasSuggestion,
    )
"""
